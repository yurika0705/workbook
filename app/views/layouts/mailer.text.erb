fruits_price = [["apple", [200, 250, 220]], ["orange", [100, 120, 80]], ["melon", [1200, 1500]]]

fruits_price.each do |fruit| ←ruits_priceから["apple", [200, 250, 220]]という値を取り出し、変数fruitに代入
  sum = 0
  fruits[1].each do |price|  ←fruitの1番目の値[200, 250, 220]から値を1つずつ取り出す
    sum += price             ←自己代入しながらsumを出力
  end
  puts "#{fruit[0]}の合計金額は#{sum}円です"
end